---
trigger: always_on
---

* The Server is God: The server is the single source of truth. Clients are dumb terminals that only display state and send raw inputs.

* Never Trust the Client: All validation must happen on the server. The client should never validate moves or actions before sending them.